import styled from 'styled-components';
import { colors } from 'styles/colors';

export const PageLayoutFull = styled.section`
  width: 100%;
  margin: auto;
  color: ${colors.black};
  background-color: ${colors.gray};
`;
